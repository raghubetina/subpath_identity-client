class CreateLocalProfiles < ActiveRecord::Migration[<%= ActiveRecord::Migration.current_version %>]
  def change
    create_table :local_profiles do |t|
      t.integer :global_user_id, null: false
      t.string :root_cache_key
      # Tombstone marker: set when the provider reports the account gone.
      # SyncLocalProfile keeps a revoked row (with its cached columns
      # blanked) rather than deleting it, so a stale in-flight fetch
      # can't resurrect a closed account. See that concern's comments.
      t.datetime :revoked_at

      t.timestamps
    end

    add_index :local_profiles, :global_user_id, unique: true
  end
end
